To decrypt the `server.py.age`, use the command:

```bash
age --decrypt server.py.age
```

and the password is the flag of the `Pedantic` challenge.

The `age` command is from https://github.com/FiloSottile/age
